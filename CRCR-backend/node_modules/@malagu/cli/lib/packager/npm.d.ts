export declare class NPM {
    get lockfileName(): string;
    get copyPackageSectionNames(): never[];
    get mustCopyModules(): boolean;
    getProdDependencies(cwd: string, depth: number): Promise<any>;
    readLockfile(lockfilePath: string): Promise<any>;
    writeLockfile(lockfilePath: string, content: any): Promise<void>;
    _rebaseFileReferences(pathToPackageRoot: string, moduleVersion: string): string;
    /**
     * We should not be modifying 'package-lock.json'
     * because this file should be treated as internal to npm.
     *
     * Rebase package-lock is a temporary workaround and must be
     * removed as soon as https://github.com/npm/npm/issues/19183 gets fixed.
     */
    rebaseLockfile(pathToPackageRoot: string, lockfile: any): any;
    install(cwd: string): Promise<any>;
    prune(cwd: string): Promise<any>;
    runScripts(cwd: string, scriptNames: string[]): Promise<any[]>;
}
//# sourceMappingURL=npm.d.ts.map