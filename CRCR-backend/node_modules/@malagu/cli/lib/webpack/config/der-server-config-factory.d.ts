import { CliContext } from '../../context';
export declare class DevServerConfigFactory {
    create(config: any, context: CliContext, target: string): {
        devServer: {
            writeToDisk: boolean;
            stats: string;
            watchOptions: {
                ignored: RegExp;
            };
            port: any;
            open: any;
            contentBase: boolean;
        };
    } | {
        devServer: {
            stats: string;
            watchOptions: {
                ignored: RegExp;
            };
            port: any;
            open: any;
            contentBase: boolean;
        };
    };
    support(context: CliContext, target: string): boolean;
}
//# sourceMappingURL=der-server-config-factory.d.ts.map