import { CliContext } from '../../context';
export declare class ComponentConfigFactory {
    create(config: any, context: CliContext, target: string): {
        resolveLoader: {
            modules: string[];
        };
        module: {
            rules: {
                test: RegExp;
                use: {
                    loader: string;
                    options: {
                        target: string;
                        modules: unknown[];
                    };
                };
            }[];
        };
    };
    support(context: CliContext, target: string): boolean;
}
//# sourceMappingURL=component-config-factory.d.ts.map