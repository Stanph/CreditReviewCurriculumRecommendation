import { Session, SessionStrategy, SessionStore } from './session-protocol';
export declare class CookieSessionStore implements SessionStore {
    protected session: Session | undefined;
    protected readonly sessionOptions: any;
    protected readonly sessionStrategy: SessionStrategy;
    get(id: string): Promise<Session | undefined>;
    set(session: Session): Promise<void>;
    remove(id: string): Promise<void>;
}
//# sourceMappingURL=session-store.d.ts.map