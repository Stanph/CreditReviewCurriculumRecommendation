import { SessionManager, Session, SessionStrategy, SessionStore } from './session-protocol';
export declare class SessionManagerImpl implements SessionManager {
    protected readonly sessionIdKey: string;
    protected readonly sessionStrategy: SessionStrategy;
    protected readonly sessionStore: SessionStore;
    protected getSessionId(): Promise<string | undefined>;
    get(): Promise<Session>;
    remove(): Promise<void>;
    commit(): Promise<void>;
}
//# sourceMappingURL=session-manager.d.ts.map