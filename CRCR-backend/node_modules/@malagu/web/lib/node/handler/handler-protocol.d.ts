import { Middleware } from '../middleware';
export declare const HandlerExecutionChain: unique symbol;
export declare const HandlerAdapter: unique symbol;
export declare const HandlerMapping: unique symbol;
export interface HandlerExecutionChain {
    execute(middlewares: Middleware[]): Promise<void>;
}
export interface HandlerAdapter {
    readonly priority: number;
    handle(): Promise<void>;
    canHandle(): Promise<boolean>;
}
export interface HandlerMapping {
    handle(): Promise<void>;
}
//# sourceMappingURL=handler-protocol.d.ts.map