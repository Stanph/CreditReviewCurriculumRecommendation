import { HandlerMapping, HandlerAdapter } from './handler-protocol';
export declare class HandlerMappingImpl implements HandlerMapping {
    protected readonly handlerAdapters: HandlerAdapter[];
    protected prioritized: HandlerAdapter[];
    constructor(handlerAdapters: HandlerAdapter[]);
    handle(): Promise<void>;
}
//# sourceMappingURL=handler-mapping.d.ts.map