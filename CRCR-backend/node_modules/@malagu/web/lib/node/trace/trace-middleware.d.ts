import { TraceIdResolver } from './trace-protocol';
import { Middleware } from '../middleware';
import { Context } from '../context';
import { Logger } from '@malagu/core';
export declare class TraceMiddleware implements Middleware {
    protected readonly traceIdResolver: TraceIdResolver;
    protected readonly logger: Logger;
    handle(ctx: Context, next: () => Promise<void>): Promise<void>;
    readonly priority = 2200;
}
//# sourceMappingURL=trace-middleware.d.ts.map