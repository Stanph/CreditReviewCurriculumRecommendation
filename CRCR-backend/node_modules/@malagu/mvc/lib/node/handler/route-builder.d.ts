import { ControllerMetadata } from '../annotation/controller';
import { StrOrRegex } from '../annotation/method';
import { ErrorType } from '@malagu/core';
import { PathResolver } from '@malagu/web';
import { RouteMetadata } from './handler-protocol';
export declare class RouteBuilder {
    protected readonly controllers: any[];
    protected readonly pathResolver: PathResolver;
    build(): Promise<{
        mapping: Map<string, Map<StrOrRegex, RouteMetadata>>;
        errorMapping: Map<ErrorType, any>;
    }>;
    protected doBuildRouteMap(mapping: Map<string, Map<StrOrRegex, RouteMetadata>>, targetConstructor: any, controller: any, controllerMetadata: ControllerMetadata): Promise<void>;
    protected doBuildErrorRouteMap(errorMapping: Map<ErrorType, RouteMetadata>, targetConstructor: any, controller: any, controllerMetadata: ControllerMetadata): Promise<void>;
    protected doRouteMetadata(targetConstructor: any, method: string): {
        paramMetadata: any;
        bodyMetadata: any;
        queryMetadata: any;
        requestHeaderMetadata: any;
        responseHeaderMetadata: any;
        requestCookieMetadata: any;
        responseCookieMetadata: any;
        requestSessionMetadata: any;
        responseSessionMetadata: any;
        viewMetadata: any;
    };
}
//# sourceMappingURL=route-builder.d.ts.map