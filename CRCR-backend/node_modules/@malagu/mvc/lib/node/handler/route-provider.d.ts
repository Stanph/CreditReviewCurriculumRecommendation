import { Deferred } from '@malagu/core';
import { Route, RouteProvider } from './handler-protocol';
import { RouteBuilder } from './route-builder';
export declare class RouteProviderImpl implements RouteProvider {
    protected route: Route;
    protected routeDefered: Deferred<Route>;
    protected readonly routeBuilder: RouteBuilder;
    protected init(): Promise<void>;
    provide(): Promise<Route>;
}
//# sourceMappingURL=route-provider.d.ts.map