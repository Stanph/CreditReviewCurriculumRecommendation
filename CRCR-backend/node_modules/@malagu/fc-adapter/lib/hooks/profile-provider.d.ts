export interface Profile {
    accountId: string;
    accessKeyId: string;
    accessKeySecret: string;
    defaultRegion: string;
    protocol: string;
}
export declare class ProfileProvider {
    provide(quiet?: boolean): Promise<Profile>;
    protected getProfile(): Promise<Profile>;
    protected isAllRequiredExist(profile: Profile): boolean;
    protected extract(regex: RegExp, endpoint: string): string | undefined;
    protected cleanObject(target: any): any;
    protected extractProfile(endpoint: string): {
        accountId: string | undefined;
        defaultRegion: string | undefined;
        protocol: string;
    };
    protected extractProtocol(endpoint: string): "https" | "http";
    protected getProfilePath(): string;
    protected getProfileFromDotEnv(): Promise<Profile>;
    protected getProfileFromFile(): Promise<Profile>;
    protected getProfileFromEnv(): Promise<Profile>;
    protected mark(source: string): string | undefined;
    protected promptForProfile(): Promise<void>;
}
//# sourceMappingURL=profile-provider.d.ts.map